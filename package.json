{
  "name": "@absolutejs/attribution",
  "version": "0.3.1",
  "description": "Privacy-aware click attribution, resilient browser measurement, and durable server conversion delivery for AbsoluteJS applications.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/absolutejs/attribution.git"
  },
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    },
    "./google-ads": {
      "types": "./dist/googleAds.d.ts",
      "import": "./dist/googleAds.js",
      "default": "./dist/googleAds.js"
    },
    "./reddit": {
      "types": "./dist/reddit.d.ts",
      "import": "./dist/reddit.js",
      "default": "./dist/reddit.js"
    }
  },
  "files": [
    "CHANGELOG.md",
    "LICENSE",
    "README.md",
    "changelog.json",
    "dist"
  ],
  "license": "Apache-2.0",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rm -rf dist && bun build src/index.ts src/googleAds.ts src/reddit.ts --outdir dist --root src --sourcemap --target=browser && tsc --project tsconfig.build.json",
    "format": "prettier --write \"./**/*.{ts,json,md}\"",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "check:package": "bun run format && bun run typecheck && bun run test && bun run build && absolute-changelog check",
    "release": "bun run check:package && npm publish --access public",
    "prepublishOnly": "bun run check:package"
  },
  "devDependencies": {
    "@absolutejs/changelog": "^0.6.0",
    "@types/bun": "1.3.14",
    "prettier": "3.9.6",
    "typescript": "5.9.3"
  }
}
